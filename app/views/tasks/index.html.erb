<h1>Tasks!!!!!!</h1>

<ul>
  <h5>Here are the tasks you still need to complete:</h5>
  <% @not_done_tasks.each do |task| %>
    <li><a href="/tasks/<%= task.id %>"><%= task.name %></a></li>
    <form action="/tasks/<%= task.id %>" method="post">
      <input name="_method" type="hidden" value="patch">
      <input name="done" type="hidden" value="true">
    <button>Mark done</button>
    </form>
  <% end %>
  <h5>Here are your completed tasks:</h5>
   <% @done_tasks.each do |task| %>
    <li><a href="/tasks/<%= task.id %>"><%= task.name %></a></li>
  <% end %>

</ul>
<h4>NOW GET BUSY!!!!!!</h4>
<h6> Nobody got time for dat! </h6>
<br />
<br />

<p>Click <a href="/tasks/new">here</a> to add a new task</p>

